---
/**
 * WalledGarden.astro
 * Abstract illustration of personal data ownership
 */
---

<div class="ownership-diagram w-full max-w-sm mx-auto py-8">
    <svg viewBox="0 0 300 300" class="w-full h-auto" xmlns="http://www.w3.org/2000/svg">
        <!-- Outer orbit ring -->
        <circle cx="150" cy="150" r="110" fill="none" stroke="#6B705C" stroke-width="2" stroke-dasharray="8,6" opacity="0.4"/>
        
        <!-- Inner protective circle -->
        <circle cx="150" cy="150" r="75" fill="#6B705C" fill-opacity="0.08" stroke="#6B705C" stroke-width="2.5"/>
        
        <!-- Central key icon (ownership symbol) -->
        <g transform="translate(120, 115)">
            <!-- Key head (circular) -->
            <circle cx="30" cy="20" r="18" fill="none" stroke="#E9C46A" stroke-width="4"/>
            <circle cx="30" cy="20" r="8" fill="#E9C46A"/>
            <!-- Key shaft -->
            <rect x="26" y="38" width="8" height="40" fill="#E9C46A"/>
            <!-- Key teeth -->
            <rect x="34" y="58" width="12" height="6" fill="#E9C46A"/>
            <rect x="34" y="68" width="8" height="6" fill="#E9C46A"/>
        </g>
        
        <!-- Orbiting data nodes -->
        <g class="data-nodes">
            <!-- Node 1 - top -->
            <circle cx="150" cy="40" r="12" fill="#CB997E" stroke="#2D2D2D" stroke-width="2"/>
            <circle cx="150" cy="40" r="5" fill="#FDFBF7"/>
            
            <!-- Node 2 - right -->
            <circle cx="260" cy="150" r="12" fill="#CB997E" stroke="#2D2D2D" stroke-width="2"/>
            <circle cx="260" cy="150" r="5" fill="#FDFBF7"/>
            
            <!-- Node 3 - bottom -->
            <circle cx="150" cy="260" r="12" fill="#CB997E" stroke="#2D2D2D" stroke-width="2"/>
            <circle cx="150" cy="260" r="5" fill="#FDFBF7"/>
            
            <!-- Node 4 - left -->
            <circle cx="40" cy="150" r="12" fill="#CB997E" stroke="#2D2D2D" stroke-width="2"/>
            <circle cx="40" cy="150" r="5" fill="#FDFBF7"/>
        </g>
        
        <!-- Connection lines from center to nodes -->
        <g stroke="#6B705C" stroke-width="1.5" stroke-dasharray="4,4" opacity="0.5">
            <line x1="150" y1="75" x2="150" y2="52"/>
            <line x1="225" y1="150" x2="248" y2="150"/>
            <line x1="150" y1="225" x2="150" y2="248"/>
            <line x1="75" y1="150" x2="52" y2="150"/>
        </g>
    </svg>
    
    <p class="text-center font-mono text-xs text-charcoal-ink/60 mt-4">Your key. Your data. Your control.</p>
</div>

<style>
    .ownership-diagram svg {
        filter: drop-shadow(3px 3px 0px #2D2D2D);
    }
    
    .data-nodes {
        animation: slow-spin 30s linear infinite;
        transform-origin: 150px 150px;
    }
    
    @keyframes slow-spin {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }
</style>
